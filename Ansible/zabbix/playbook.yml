---
- name: Install zabbix agent on RHEL
  hosts: all
  become: true
  vars:
    zabbix_agent_server: []
    zabbix_agent_serveractive: "10.10.10.10" 
    zabbix_agent_hostname: "SUDIP-DND"
    zabbix_agent_startagents: 0
    zabbix_agent_timeout: 30
    zabbix_repo_package: true
    # zabbix_agent_listeners: []
    # zabbix_agent_listeners_tls: []
  roles:
    - role: community.zabbix.zabbix_agent
